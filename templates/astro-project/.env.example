# Vercel hosts this project. Set these env vars in the Vercel dashboard
# (Project Settings -> Environment Variables), scoped per environment
# (Production / Preview / Development), OR keep a local .env for `vercel dev`.
# `provision-vercel.sh` pushes them via `vercel env add`. See
# references/hosting-vercel.md.

# NO CMS is wired in this build: content lives in src/lib/content.ts and is read
# through loadPage(). `scripts/add-sanity.sh` adds the SANITY_* vars here when a
# CMS is added. See references/cms-and-draft-preview.md.

# "true" during the EXPLORE stage - shows the top-bar variant switcher and
# the section-ID labels on each variant. Flip to "false" at Compose time.
# On Vercel: set per-environment (Preview: true during Explore, then false).
# See references/explore-stage.md.
PUBLIC_EXPLORE_MODE=false

# Runtime - read by /api/contact via process.env.
RESEND_API_KEY=
TURNSTILE_SECRET=

# Runtime - the post-deploy form round trip. `x-palate-smoke: 1` makes /api/contact validate
# and answer without sending; on a PRODUCTION deployment that header is ignored unless
# `x-palate-smoke-secret` matches this value, and a blank value refuses every smoke request.
# Set it on Production only, and set the same value in whatever runs verify-vercel.sh or
# verify-cloudflare.sh. Leave it unset on preview: there the header works on its own.
# The provisioning scripts GENERATE this and write it here, so leave it commented out: a blank
# assignment is not the same as an absent one, and one sitting above the generated line is what
# made a reader take the wrong value.
# See references/testing.md, "The form round-trip test".
# PALATE_SMOKE_SECRET=

# Vercel injects these automatically per deployment (listed for reference):
#   VERCEL_ENV       - "production" | "preview" | "development"
#   VERCEL_URL       - the deployment URL
#   VERCEL_GIT_COMMIT_SHA, VERCEL_REGION, VERCEL_PROJECT_ID, VERCEL_TARGET_ENV
