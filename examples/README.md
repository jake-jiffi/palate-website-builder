# Examples

See references/examples.md for invocation patterns. A full worked example will be added here after the first pilot validates the package gate.

## The pilot gate
The first pilot answers one question: does the private brand package install in CI and deploy cleanly to Cloudflare Workers? If yes, the package path is the default. If no, --vendor-brand becomes default. This is the single most important thing the first build proves.
