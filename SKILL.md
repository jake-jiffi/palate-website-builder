---
name: palate-website-builder
description: >
  The Palate website builder skill. Builds AND maintains production-grade Astro websites grounded by the Palate MCP (2,000+ deeply-analysed real websites with inner-page depth, design tokens, and a taste layer). Three modes: (1) BUILD SITE, scaffold a new site grounded by the Palate MCP for real design taste. (2) CONTINUE SITE, extend an existing site (add a page or section, restyle, add a feature) with the same MCP grounding and gates, minus the scaffold and identity machinery. (3) BUILD BRAND, convert raw brand assets into a four-format design token package. Trigger when asked to build, scaffold or start a website; to add to, edit, restyle, extend, continue or improve an existing site; or to build/convert a brand package. (4) RUN SITE, the ongoing content runtime: write or edit a blog post, news item, case study or changelog entry; change a business fact such as opening hours, a price, a phone number, an address or a service; add or replace an image; preview, publish, unpublish, schedule or roll back a change; check or grade a site; report on what changed; answer questions about a site's own content; or bring an existing site under management. Trigger on any of those too, including plain requests like "write a post about X", "update our opening hours", "put this live", "what changed this month" or "which pages mention the old price".
allowed-tools: [Read, Write, Edit, Glob, Grep, Bash, Skill, WebSearch, WebFetch, mcp__palate]
---

# Palate Website Builder

One skill, three capabilities. It builds production-grade Astro websites grounded by the Palate MCP's library of deeply-analysed real websites, keeps building them after launch (add pages, sections and features with the same rigour), and builds the brand packages that feed those builds. The Palate MCP provides the taste; this skill provides the craft.

## RUN SITE: route content work to a command, do not improvise it

**Read this before the mode table.** If the request is ongoing work on a site that already exists,
the procedure is already written and you must follow it rather than invent one. Read the named file
under `commands/` and do what it says.

**Route on the verb they used, not the noun they mentioned.** "Get the new hours out now" is a
publish, not a fact change. "Take out the bit about free consults" is an edit, not a service change.

| If the person says... | Read and follow |
|---|---|
| reword this, rewrite it, fix the typo, "take out the bit about...", "it still says X" (the words on a page they can name) | `commands/edit.md` |
| a business value, changed once and everywhere it appears: phone, email, address, opening hours, prices, service areas, the services list, the trading name | `commands/fact.md` |
| add a page, add a proper section, another location or service page, put something in the top menu | `commands/page.md` |
| a dated item: news, a blog post, a case study, a changelog entry, "there's been nothing new on there since March" | `commands/post.md` |
| add, replace or swap a photo, wrong picture, "the images are wrong", a page crawling because the imagery is heavy | `commands/image.md` |
| an offer with a deadline and somewhere to send people: "20% off til end of month, front and centre", a seasonal push, the page an ad points at | `commands/campaign.md` |
| every page, everywhere, "the lot", "not just the one", "top to bottom", or the monthly pass (crawlability, orphans, dead links, stale content) | `commands/sweep.md` |
| show me, let me see it, "can the client look at it first", "how do they sign off before it goes live" | `commands/preview.md` |
| put it live, ship it, "get it out now", and also "they can't find it" / "it's still not showing up" (it was never published) | `commands/publish.md` |
| go live at a named time: Thursday, the 1st, next Monday morning | `commands/schedule.md` |
| take it down, get rid of it, "it shouldn't be findable", we don't do that any more so lose the page | `commands/unpublish.md` |
| undo what we just did, put the previous version back, "back to how it was" | `commands/rollback.md` |
| is anything broken, did that break something, a customer hit a fault, "make sure nothing's gone sideways", give it a once-over before it goes out | `commands/check.md` |
| has this page moved from where we left it, is it off-brand, "has anything drifted" | `commands/drift.md` |
| the number, the score, is this any good, "how do we compare to them" (ours or a competitor's URL). Grades locally and free by default, deployed or not; certification is opt-in | `commands/grade.md` |
| why, how come, who decided, "what happened to...", "where did X go", "I don't remember asking for X", and why something failed or was blocked | `commands/why.md` |
| what, where, when, does it: "which pages mention X", "when did we last post", "have we got a page for Y" | `commands/ask.md` |
| where is this site up to right now, what is in flight, what is waiting on me, and "is that live yet" / "did yesterday's changes actually go up" (a state question, not a fault) | `commands/status.md` |
| is the published site answering at all, can anyone reach it, has it stopped serving | `commands/status.md --live` |
| what changed this month, catch me up, "remind me what we've done", the monthly report | `commands/report.md` |
| record a decision or a standing preference, with no change to make right now | `commands/remember.md` |
| bring an existing site we did not build under management | `commands/adopt.md` |
| hand the site on to whoever takes it next, client or colleague: the accounts, the ownership, the written record of where things stand, our access revoked | `commands/handover.md` |
| set this machine or the tooling up | `commands/setup.md` |

### Follow-ons carry the last route

"Same again", "do that one too", "the other one as well", "just the highlights, not the whole essay":
these carry no route of their own. **Reuse the command from the previous turn with the new target.**
Only ask if there is no previous turn to carry.

When the new target is the whole site, what to do depends on what is being carried, and getting
this wrong is silent. **Widen to `sweep.md` only when the carried command was itself a check.**
When it was a change to one page, widening means making that change on each page: name the list
and confirm it. Running the site audit instead would answer a question nobody asked and leave the
change unmade.

### Scope beats topic

One page they can name goes to `edit.md`, `fact.md` or `image.md`. Every page, or a cold look over
the lot, goes to `sweep.md`. **`fact.md` is the exception**: it is already site-wide, so "the new
number is on heaps of pages" is still `fact.md`, never a sweep.

### People report symptoms, not commands. Route to the fix

Something is missing that should be public: `publish.md`. Nothing has been added for months:
`post.md`. Slow to load: `image.md`.

**A reported fault splits three ways, and the wrong one wastes their time at the worst moment.**

- **They are asking why, or whether they caused it** ("how come", "was that us", "is it meant to
  look like that"): `why.md`. It reads the recorded decision and then the commit, and says plainly
  when nothing was recorded rather than inventing a reason.
- **Something in the working tree might be at fault**: `check.md`. It is scoped to the diff, so it
  is the right answer only while the suspect change is still uncommitted. **If the work already
  shipped there is no diff and check returns nothing**, which reads as "all clear" and is not.
- **No recent change, or the complaint is about the site as a whole** (nobody is getting through,
  something has been wrong for weeks): `sweep.md`. Whole-site faults, an unreachable page, a
  blocked crawler, a link to nowhere, are invisible to anything scoped to a diff.

Nothing in the set submits a form or places a call, so a broken enquiry path can be reasoned about
but not reproduced. Say that rather than implying it was tested.

### When two rows both fit

- **edit vs fact.** They quote the words: `edit.md`. They hand you a new value: `fact.md`.
- **page vs post.** It belongs in the menu forever: `page.md`. It is dated and the next one pushes it down: `post.md`.
- **fact vs page vs unpublish, for services.** A line in the services list: `fact.md`. A service with its own page, added: `page.md`, removed: `unpublish.md`.
- **check vs drift vs grade.** Is it broken: `check.md`. Has it moved off its baseline: `drift.md`. Give me the number: `grade.md`.
- **ask vs why.** What, where, when: `ask.md`. Why, who, how come: `why.md`, and that includes changes and removals, not only failures.
- **status vs report.** Right now: `status.md`. Over a period, or "what have we done": `report.md`.
- **unpublish vs rollback.** Should the thing be gone: `unpublish.md`. Should it exist, as it was: `rollback.md`.
- **remember vs doing it.** If there is a change to make now, make it and record it after. `remember.md` alone is for a decision with no work attached.

### Ask instead of guessing

Name the two candidates in the question. Never ask an open one.

- Two actions in one: "take it down and put the old one back".
- A deadline that might be a publish date: "can you get it ready for Monday".
- Something new that could be a list entry or its own page: "we've started doing pet dental".
- A handover with no mention of accounts or ownership, which may be a written brief rather than a
  transfer. If they name the logins, the domain or the accounts, it is `handover.md`, and it does
  not matter whether the recipient is a client or a colleague.
- A follow-on with no previous turn to carry.

If nothing above fits, say so and ask. A wrong command is more expensive than a question.

## Mode dispatch (read this first)

Decide which mode the request is, then jump to that section. When a request spans modes (a full build that also needs a brand package), run the modes in the order the build pipeline calls for.

| If the request is... | Mode | Go to |
|----------------------|------|-------|
| "build/scaffold/start a site for {client}", "migrate {client} from Webflow" | **BUILD SITE** | "Mode: Build a website" |
| "add a page/section to {site}", "edit / restyle / improve this site", "continue building {site}", "build the {pageType} page on my existing site" | **CONTINUE SITE** | "Mode: Continue an existing site" |
| "build a brand repo / brand package for {client}", "convert these brand assets", "turn this brand into code" (no website) | **BUILD BRAND** | "Mode: Build a brand package" |

The three modes share infrastructure (slug derivation, the Palate MCP reference library, the gates, state patterns) but produce different things. BUILD SITE may call BUILD BRAND internally (Phase 0) when a site needs a brand package that doesn't exist yet; CONTINUE SITE reuses the MCP grounding and the gates on an existing codebase, without the scaffold or identity machinery.

For adding to, editing, restyling or extending an EXISTING site, use CONTINUE SITE mode (below), not a fresh BUILD SITE. Do not use this skill for pure web applications (a browser app, not a website), or to hand-write a throwaway HTML mockup with no project (the freestyle anti-pattern). A small or single-page site IS in scope: build it as a real Astro project, not loose HTML.

---

## Mode: Build a website

Brief plus domain in, real Astro site out. The build runs in two stages on ONE codebase, and the process below is mandatory, not advisory (see "The non-negotiable build contract").

### Two stages, one codebase
A build has a `stage`, recorded in `.palate-skill-state.json`:

- **preview** (the default for "redesign this", "make it look good", "let me see it first"): runs Phase 0 (brand) + Phase A (scaffold the real SSR Astro project) + the preview verification gate, then STOPS. Deliverable: a **shareable Vercel preview deployment** (a live `*.vercel.app` link the client can open and leave Vercel Toolbar Comments on) via `scripts/deploy-preview.sh` - this is the default so previews are shareable for feedback. The preview runs on the bundled `content.ts` content layer, so NO CMS, custom domain, GitHub repo or PRODUCTION deploy is touched (a preview deployment is throwaway) - the SSR adapter, the Sanity data layer, the draft-mode endpoints and the visual-editing channel are all already wired in. Use `--local-preview` for a local dev-server link instead (faster for solo iteration, no cloud). This is the real production codebase, just not provisioned; promoting it later is a continuation, never a rebuild.
- **production** ("make it production-ready", "take it live", "ship it"): runs `scripts/promote-to-production.sh` (which re-checks the preview is a real Astro project) then continues Phases B-F on the SAME project - deploying the SSR worker and, **only if the build needs a CMS**, adding Sanity (`scripts/add-sanity.sh`), provisioning it and seeding the dataset from `content.ts`. Never starts over.

A request that says nothing about stage defaults to preview. At the end of a preview, hand over the `SHAREABLE_URL` from `deploy-preview.sh` - an open-for-anyone link (the script auto-enables Vercel Protection Bypass via the CLI, so the client needs no login) - and note that "make it production-ready" promotes it. With `--local-preview`, hand over the local `npm run dev` link instead.

### The non-negotiable build contract (this is what makes it production-grade)
These are hard requirements. If any cannot be met, STOP and say so. NEVER substitute a shortcut.

1. **The deliverable is a real Astro project scaffolded from `templates/astro-project/`.** Never hand-write HTML/CSS/JS files as the deliverable. Loose `.html` files at the project root are the freestyle anti-pattern and are forbidden.
2. **Preflight runs first and must pass** (`scripts/preflight.sh`). If tools are missing, install them (or instruct/auto-install per environment); do not proceed half-configured.
3. **Phase A scaffolds from the template, then `scripts/verify-is-real-astro.sh` MUST pass** before the preview is considered done. This gate fails the build if the output is not a genuine Astro project that compiles. It is the anti-freestyle checkpoint.
4. **Every phase records state and verifies before being marked complete.** No skipping, no "I'll just do the visible part".
5. **The brand system comes from the brand package (or vendored brand), applied through the template's BaseLayout and Tailwind preset.** Not ad-hoc inline styles invented per page.
6. **Resolve the working directory by detection, never by assumption.** Run `detect-environment.sh` first and build under the WORK_ROOT it returns. Do not hardcode `/mnt/...` paths, do not assume a folder is writable, never build inside the skill's own directory. Establish a real, user-visible project folder up front (`request_cowork_directory`) so the deliverable is never stranded in a temp session folder the person cannot find.
7. **The site is SSR from the first file, and has NO CMS until one is needed.** Server-rendered on the host (Vercel by default, Cloudflare as the backup) with every page reading through `loadPage()`. NEVER ship a static preview and retrofit SSR later - that IS the rebuild this rule exists to prevent. But do not carry a CMS by default either: content is authored in `src/lib/content.ts`, the Sanity tree costs ~850 packages, and most sites never need it. Because output stays `"server"` and no page ever imports `content.ts` directly, adding a CMS later is purely additive: `scripts/add-sanity.sh <dir>` swaps two no-op seam files and edits no page. **Add one only when the client will edit their own copy, or content is collection-shaped and growing (blog, case studies, menu, listings).** Ask it at the plan checkpoint in the client's terms: *who edits this copy in six months?* See `references/build-stages.md` and `references/cms-and-draft-preview.md`.
8. **Draft preview is standard WHEREVER there is a CMS.** If a build has Sanity, the Presentation tool showing unpublished edits comes with it, not as an add-on - `add-sanity.sh` wires it. A build with no CMS has no drafts to preview, and that is the expected shape, not a gap. Exact config in `references/cms-and-draft-preview.md`.
9. **MODE-AWARE DIVERGE comes first for every new site / landing page, before any source is written.** The build's first creative act is to detect the brand mode (was a brand provided?) then sample 8-10 genuinely different directions distinct on the axes for that mode, self-tagging each concept's conventionality / lens / cross-domain analogical seed, then CONVERGE as a CURATOR: cull only what cannot be built, and assign the survivors to an ambition ladder spanning genuinely-understated to genuinely-bold. **It does not narrow to one or two.** Every variant carries its own concept; N dressings of one spine is the regression this replaced. **brand-creation** (no brand provided): diverge the full identity space - colourway + type/faces + mood + layout + motion (you are helping CREATE the brand). **brand-provided** (a brand exists): LOCK the brand colour + type + identity and diverge ONLY layout + composition + section logic + motion + density + art direction WITHIN the brand; never vary colour or fonts away from a provided brand. This is ENFORCED, not advisory: the PreToolUse gate BLOCKS the first `.astro`/`.svelte`/`.vue`/`.tsx`/`.css` write of a build site until `build-manifest.json` records a valid `diverge` for the build's brand mode (>= 8 concepts spanning the conventionality range with a low-typicality tail, distinct on the mode's axes: brand-creation needs >= 3 distinct colourways AND type directions, brand-provided needs colour/type LOCKED and >= 6 distinct layout/motion skins) and `converge` (>= 1 advanced), and the done gate catches a build site that skipped it. A build cannot proceed to source without it. EXPLORE then builds one home-page variant per curated concept, in ladder order (some reference-led, some invented), the count agreed at the plan checkpoint and defaulting to 8; the client picks what to combine; Compose builds the canonical pages from those picks (same Astro project throughout, variants ARE the project at an earlier shape). Skip only for tiny edits, calm / conversion briefs, or briefs that name an existing direction - NOT for a HIGH-INTENSITY commission, where built Explore is mandatory (a bold brief that collapses Explore to one concept ships Variety-flat; `manifest.commission.intensity == "high"` makes built Explore required and the done gate fails a high-intensity build with `variants: []` unless `commission.explore_skip` records a named-direction reason). See `references/story-engine.md` then `references/explore-stage.md`.
10. **Mechanical and interpretive aesthetic gates run PER EXPLORE VARIANT, and again at Compose, Finalise and Production.** Per variant is the one that was missing and it is the one that matters most: variants are what a client sees FIRST, and a gate that only runs at Compose fires after they have already looked. The mechanical gate is `scripts/ux-lint.sh`, which parses `references/anti-patterns.md` and enforces house style (no em dashes), banned fonts and gradients, AI tells in copy, and the Vercel-derived code-level rules. The interpretive gate is the Phase A.10 reviewer pass against `references/audit-dimensions.md`. Both must pass with no Critical or High findings before the preview is handed over and again before production provisioning. The verify-fail loop in `references/errors.md` applies (read, diagnose, fix the real cause, re-run, max 3 attempts, then HALT). Per-line escape: `ux-lint-disable <rule-id>` comment on the same or preceding line.
11. **THE CLIENT'S PHOTOS DECIDE THE LAYOUT, NOT THE OTHER WAY ROUND. Measure and VIEW them before choosing any treatment.** On any rebuild, modernisation or brand carry-over, run `node "${CLAUDE_PLUGIN_ROOT}/scripts/palate-assets.mjs" <assets-dir>` FIRST: it writes `.palate/assets.json` with every photo's dimensions, orientation, the largest slot it can fill before it goes soft, and the crop loss for each common slot ratio. **Choosing a ratio and pouring their photos into it is the most reliable way to make a good build look cheap.** A build once set `aspect-ratio: 3/1` full-bleed without opening the set: 15 of 31 photos were portrait, a 2:3 portrait through a 3:1 letterbox shows **22% of the frame**, and a candid of two people at a counter shipped as two decapitated torsos centred on a t-shirt, with every gate green because nothing had measured anything. Then **LOOK at each image**, because pixels cannot see the subject: a crop that keeps two faces and one that slices them measure identically. Record `subject` (what `object-position` should be set from; centre is only right when the subject is centred) and `treatment` (its slot, and where it must NOT go). `--check` exits 1 while any image in use is unreviewed, so "nobody looked" is a visible failure. `verify-rendered` fails an image shown larger than it exists, and runs all three viewports because a photo that survives the desktop crop is often destroyed by the mobile one. Full doctrine: `references/assets.md`.
12. **Critique discipline runs at variant-time and compose-time.** Every variant begins with a stated Design Read and carries a demonstrative concept from the Story Engine (`references/story-engine.md`); Compose runs the 6-axis pre-emit critique, the anti-default detector, the feel gate (would the ICP feel seen) and the Conceptual Grounding Test before emit; the lead reference's signature move must appear in the build (re-skinned, named and located); Claude's own narration avoids the vague-word ban list. See `references/critique-discipline.md`.

If the environment genuinely cannot run Node/Astro (so no real scaffold is possible), the correct action is to STOP and tell the person what is needed, NOT to produce a static HTML mockup and present it as the deliverable.

### The plan checkpoint (careful work only)

Before substantial or irreversible work, show a short plan and get a go-ahead. This is NOT a gate on every action - it is for four moments:

1. **Before Phase A starts.** Show, in a few lines: the pages to build, the brand source (package / composed / vendored), the references chosen, the industry (steers per-industry rules via `references/industry-patterns.md`), the stack and the host (**Vercel is the default; Cloudflare is the backup** - always ASK which to use, stating Vercel as the default, even on a quick build; see `references/hosting-vercel.md`), whether Explore is on (default YES for new sites / landing pages - see `references/explore-stage.md`), the stage (preview or production - note a preview is deployed to a shareable Vercel link unless `--local-preview`), and two required client-input fields: **the wow moment** (one sentence on what this site does that nothing else does) and **the avoid list** (3 to 5 explicit don'ts, e.g. "no purple", "no stock people photos"). Then ask: "Proceed?"
2. **After Explore generates the variants** (when Explore is on). The preview link is live with `/v1`..`/vN` (+ `/lp1`..`/lpN` if applicable); pause and ask which sections to combine. Mix-and-match is the default ("v3 hero, v7 features, v5 cta"); whole-variant shortcuts are fine.
3. **After Compose builds the canonical pages.** The composed `src/pages/index.astro` and the rest of the site are live in the chosen direction; pause and ask: "Proceed with the deep build?"
4. **Before production provisioning (Phase B onward).** Show what real infrastructure this creates and commits: a Sanity project, the host's worker / function, a GitHub repo, and - at Phase E - a DNS cutover. Then ask: "Proceed?"

SKIP every checkpoint for tiny, reversible work: editing an already-scaffolded project, adding one section, re-running a verify, a brief that names an existing direction ("build it like the Northwind site"). Do not make a quick edit wait on a confirmation.

Keep the plan concise - a handful of lines, not a document. Its job is to catch a wrong assumption before it costs a rebuild or real money, nothing more.

### The phases
0. **Brand**: detect `palate-projects/{slug}-brand`. For a RETURNING client, retrieve the per-client `brand-record.json` (tokens + approved type + motion-intensity band + voice) and inherit it instead of re-detecting (`references/phase-0-brand-detection.md`). If missing and raw assets exist, run BUILD BRAND in-process (composed). For a redesign, extract the real tokens from the client's existing site with the capture engine ONLY if no record exists yet, then write the record. If no assets, scaffold with `--vendor-brand` defaults. Capture the exact package version.
A. **Scaffold + Explore + Compose** (one phase, one project, several NAMED substages). The
   stage order is the spine every gate hangs off: DIVERGE -> CONVERGE -> COMMISSION -> EXPLORE ->
   per-variant + post-compose VISUAL LOOP -> mandatory VERIFY -> gates. Each substage that produces
   evidence writes its block to `build-manifest.json` (`references/build-manifest.md`).
   1. **A.1 Copy + install**: copy `templates/astro-project/`, substitute vars, commit the project .npmrc, install the brand package and deps. **Merge `_claude/` into `.claude/`** (the skill zip ships project-level hook files under `_claude/` because the sandbox protects `.claude/` from new writes - the rename is a one-step merge with the existing `.claude/CLAUDE.md.tpl`). **The template is Vercel-native (the default host) - do nothing extra for Vercel.** ONLY when the person chose `--host cloudflare`, run `scripts/switch-host-cloudflare.sh <project-dir>` after the copy: it swaps in the Cloudflare adapter, `package.json` deps, `wrangler.toml`, `public/_headers` and the wrangler CI workflows (`deploy`/`preview`/`revalidate`), and removes `vercel.json`. See `references/hosting-vercel.md`.
   1.5. **A.1.5 ARCHITECT** (the site's information architecture, before Diverge, gap4 W16). From the converged concept + the business type, derive the **page inventory** (which pages THIS site needs, e.g. home + about/services/work/pricing/contact, per the business, not a fixed template set), the **nav**, and the **journey** (the visitor's path awareness -> consideration -> conversion), each grounded in page-type coverage (`refs_search { pageType }`, see W17). Write `manifest.architecture` (`{ ran, pages:[{ route, pageType, purpose, donor_slug }], nav, journey, rationale }`). A multi-page build that writes its first INNER `.astro` with no `manifest.architecture` is a sitemap assembled by reflex, not designed - the verifier's cross-page check (8.5) flags it, falling back to the on-disk inner routes when architecture is absent so a multi-page build is never silently exempt (fail-open for a single-page / landing build; no deterministic gate-done.sh check yet). Derive the IA from the concept; never default to a template sitemap.
   2. **A.2 DIVERGE** (the concept spine, sample wide, MODE-AWARE). First detect the brand mode (was a brand provided?) and set the divergence axes from it: **brand-creation** (no brand) diverges the full identity space (colourway + type/faces + mood + layout + motion); **brand-provided** LOCKS the brand colour + type and diverges only layout + composition + section logic + motion + density + art direction within the brand. Then, from the Story Engine's one true thing + the spine, sample candidate concepts in one pass with the constraints removed (Verbalized Sampling). **Sample at least the variant count PLUS THREE** (so a set of 8 samples 11+), because CONVERGE culls on feasibility and every rung of the ambition ladder must be fillable afterwards without duplicating a neighbour. Sampling exactly N guarantees a hole, and a hole gets filled with a near-repeat of the rung beside it, which is the single most wasteful thing a preview can contain. **Sample ACROSS the ambition range deliberately**, not from one intensity: the set needs genuinely restrained candidates as much as it needs wild ones, and both ends are hard to reach from a middling prompt: each self-tags its `conventionality` (0..1, how typical it is for the category), the `lens` it came from (use 2-3 varied lenses, span >=2), a forced cross-domain `analogical_seed` (reframe the brief through an unrelated domain - print, signage, architecture, film, games), and the mode's axis fingerprints (brand-creation: `colourway` + `type`; brand-provided: `layout` + `motion` + ...). Deliberately carry the lower-typicality, higher-surprise tail forward. Writes `manifest.diverge` (`{ ran, n, mode, axes_varied, locked, concepts }`). Full doctrine in `references/story-engine.md`; the validity rule + thresholds in `references/build-manifest.md`.
   3. **A.3 CONVERGE** (curate the set, never narrow to one). **CONVERGE IS A CURATOR, NOT A FUNNEL, AND THIS IS THE CORRECTION OF A REAL REGRESSION.** It used to "advance the best 1-2 into Explore", so all N variants elaborated one or two concepts and the whole preview regressed to that concept's mean; the boldness DIVERGE had just sampled was generated and then binned. Its job is to REMOVE the unbuildable and SHAPE what survives, so Explore receives N genuinely different concepts, not N dressings of one. Steps, in order: (a) **feasibility is a FLOOR, not a weight** - drop only what cannot be built at all, computed by the buildability oracle (`references/reference-library-usage.md`): decompose each audacious moment into named mechanics, resolve each to its closest precedent + `astro_recipe`, falling back to a `references/motion-and-3d.md` recipe where the library fails closed, written to `manifest.buildability`. A harmonic mean of originality and feasibility is what used to kill the tail, because a wild concept ALWAYS scores low on buildability at concept time; scoring is now for ordering, never for elimination. (b) **assign the survivors to the ambition ladder** (below), endpoints first. (c) **enforce spread**: no two adjacent rungs sharing a `lens`, and no donor slug used twice. If a rung cannot be filled, go back to DIVERGE for more candidates at that ambition level rather than duplicating a neighbour. Run `scripts/gate-novelty.mjs --manifest build-manifest.json` as a pre-check so a safe-only set is caught (it fails-open / skips when DIVERGE did not run). Writes `manifest.converge`.

      **THE AMBITION LADDER.** The variant set is a declared RANGE, not a bag of options, because a client cannot tell you how bold they want to be until they have seen both ends. Rung 1 is the most restrained, rung N the boldest, and every rung in between is a real step. **N sets the RESOLUTION, never the range**: the endpoints are anchored at genuinely-understated and genuinely-bold whether N is 4 or 10, and the middle interpolates, so a set of 4 spans the same distance in coarser steps. Three sub-axes move together up the ladder: conceptual distance from the category default, motion and interaction intensity, and structural risk (a layout that breaks its own grid, navigation that is not a nav). **RUNG 1 IS "RESTRAINED AND EXCELLENT", NEVER "THE WEAK ONE".** It carries a signature move like every other rung; the move is simply quiet. Flawless restraint can win (`references/build-commission.md`), and a timid rung 1 reproduces at the bottom of the ladder exactly the problem the ladder exists to fix. Every rung stays ON BRIEF and ON BRAND: the top rung is the wildest expression of THIS business, never a technology demo wearing its logo. Each variant declares its rung in `src/lib/variants.ts` so the picker SHOWS the range, which is the whole point: the client points at where they want to live, or combines across rungs. The **craft-feasibility axis is computed by the buildability oracle** (`references/reference-library-usage.md`): where an advanced concept is audacious (a motion / 3D / scroll moment), decompose it into named mechanics and resolve each to its closest precedent + `astro_recipe`, falling back to a `references/motion-and-3d.md` recipe where the library fails closed; that per-mechanic verdict IS the craft-feasibility score and is written to `manifest.buildability`. Run `scripts/gate-novelty.mjs --manifest build-manifest.json` as a pre-check so a safe-only converge is caught (it fails-open / skips when DIVERGE did not run). Writes `manifest.converge`. Full doctrine in `references/story-engine.md`. Keep it lightweight: a calm conversion brief skips the oracle.
   3.5. **A.3.5 COMMISSION** (issue the brief to yourself, after the concept, before Explore). From the converged concept + the resolved brand, write the short **build commission** (`references/build-commission.md`): the ambition bar ("build a site good enough to win its category on Awwwards / FWA; competent is a fail"), the concept (the one true thing + the demonstrative mechanic + the one governing feeling), a one-line vision, the **mechanisms chosen TO FIT** from the toolkit register (a few not all, fit over familiarity, the same rule as type - calm briefs choose nothing past the Tier 0 floor), each grounded in its `references/motion-and-3d.md` recipe + its MCP precedent + `astro_recipe` (the buildability line, the same per-mechanic verdict as the oracle), the proof requirements (verify at 1440 + 390, read the pixels + the console, mobile-friendly (incl. designed mobile motion where the brand warrants it), holds 60fps, honours `prefers-reduced-motion`), and the restraint clause, which **CUTS BOTH WAYS** (maximal motion is not the bar; match intensity to the brand - a calm brand demands calm, a HIGH-INTENSITY brand (a label, a maximalist consumer brand, a creative studio, a launch, a culture / type brand) DEMANDS the **bold mandate** and a safe, flat page FAILS it: one declared feeling, copy that risks something, a hero INTERACTION not a banner (real WebGL / R3F where it earns it, a hand-built morphing-SVG-behind-a-glow where it does not), a custom cursor + tactile micro-physics, one signature easing, an entrance ritual, film-grain, rich-dark + a rare accent that detonates, scroll-as-timeline, kinetic masked type; reach into the bold-donor cluster, not the restrained flagships - see `references/build-commission.md` "The bold mandate"). Writes `manifest.commission` (descriptive fields only; the bar's pass/fail stays computed by the verifier). Keep it lightweight and CARRIED, not a heavy new ceremony: it is referenced by Explore, Compose and the verifier, not re-derived. The Awwwards / FWA bar is now the explicit ambition target those three stages hold to. It is doctrine + recorded evidence, fail-open: its absence never blocks a build.
   4. **A.4 EXPLORE** (default for new sites / landing pages; skip for tiny edits, calm / conversion briefs, or briefs that name an existing direction - NEVER skippable for a HIGH-INTENSITY commission, where built Explore is mandatory and the done gate enforces it). Set `PUBLIC_EXPLORE_MODE=true`. Generate 8-10 home-page variants as `/v1`..`/vN` (plus `/lp1`..`/lpN` if the brief warrants), **each carrying ITS OWN concept from CONVERGE's curated set, at its own rung on the ambition ladder** (never N dressings of one spine: that is the regression A.3 exists to prevent, and it is what makes a preview of eight look like a preview of one), **elaborating the commission** - the commission's chosen mechanisms are spread across the set to FIT each direction, not ticked off in every variant (`references/build-commission.md`), and every variant is held to the commission's Awwwards / FWA bar. Each is composed of section components wrapped with `<SectionMark id="vN-..."/>`; register each in `src/lib/variants.ts` with `{ id, name, href }` (give each an evocative `name`) so the bottom-right direction picker shows it. Variants must be genuinely different (some reference-led, some invented) - see `references/explore-stage.md`. **EVERY VARIANT PASSES THE FULL ANTI-AI GATE BEFORE IT IS SHOWN, AND A FAILING VARIANT IS NOT REGISTERED.** This is a hard block, not a nudge, and `PALATE_GATE_STRICT` does not govern it: the preview is the FIRST thing anyone sees of the product, so a variant that looks AI-made has already lost the argument Palate exists to win. Per variant, before it goes in `src/lib/variants.ts`: (a) `scripts/ux-lint.sh` on the variant's files - the mechanical ruleset (banned faces, gradients, the AI-tell copy list, em dashes) - **fix Critical/High and re-run, never register a variant carrying one**; (b) `scripts/verify-rendered.sh` on its route, which catches what a text rule cannot see (the eyebrow/kicker as a STRUCTURE, content buried below the fold, horizontal overflow); (c) the VISUAL LOOP (`scripts/reference-capture/screenshot-build.mjs` via `palate-verifier`) against the fixed rubric (`references/visual-rubric.md`) + `references/ai-slop-tells.md`. **Running these only at Compose (A.10) is what let AI tells reach client previews**: the lint fired after the client had already seen and picked, so the gate was downstream of the artefact it was meant to protect. A variant that cannot be cleared in 3 attempts is DROPPED and its rung resampled, never shown with a known tell. Writes `manifest.variants`. Hand over the preview URL.
   5. **A.5 PAUSE - pick.** The client says what to combine ("v3 hero, v7 features, v5 cta") or picks a whole variant.
   6. **A.6 COMPOSE.** Build the canonical `src/pages/index.astro` and the rest of the site's pages from the picked sections, adopting the dominant variant's design tokens (usually whichever supplied the hero). **Realise the commission** (`references/build-commission.md`): the composed page delivers the commission's chosen mechanisms, each built from its recorded precedent + `astro_recipe` inside the motion + reduced-motion budget (`references/motion-and-3d.md`), to the commission's ambition bar. Move variant routes into `_explore-archive/`, clear `src/lib/variants.ts`, flip `PUBLIC_EXPLORE_MODE` to `false`. **Run the post-Compose VISUAL LOOP** (the gating one) on the composed page.
   7. **A.7 PAUSE - confirm.**
   8. **A.8 Finalise**: fill `src/lib/content.ts` with the real copy, rename `src/pages/blog/slug.astro.tpl` -> `src/pages/blog/[slug].astro` if a collection is needed (Astro dynamic routes need brackets; the skill zip ships it bracket-free), apply design mode + reference-library inspiration, init git.
   9. **A.9 VISUAL LOOP + VERIFY (mandatory, fresh context).** Spawn `palate-verifier` as a subagent (isolated context) to run the visual loop (`scripts/reference-capture/screenshot-build.mjs`) over the composed page and every gate in one pass, **checking the built result AGAINST the commission** (`references/build-commission.md`): the proof contract (1440 + 390, read the pixels + the console, mobile-friendly, holds 60fps, honours `prefers-reduced-motion`), the ambition bar ("competent is a fail"), and the restraint clause (fit over maximal motion - a janky WebGL hero fails, flawless restraint can win; **and it cuts both ways: a flat page on a high-intensity brand also fails - `references/build-commission.md` "The bold mandate"**), augmenting (not replacing) the 6 axes + the defect checklist. The verifier ALSO runs the **rendered bug-class gate** (`scripts/verify-rendered.sh`, `references/rendered-bug-classes.md`): a real `mouse.wheel` scroll (JS ON + motion ON) and a JS-OFF pass at 390/834/1440 that catch the BOLD-build defects a still / reduced-motion pass MASKS - no-JS-finished-state (no JS-dismissed preloader, no blank canvas LCP), motion-on reveal stuck at opacity:0, a pinned scene that never releases, and heavy WebGL mounted on mobile. The rendered gate ALSO scores **BUILD HYGIENE** and blocks below a floor: it runs the grader's own `rubric.mjs` (vendored byte-identical, drift-guarded) over the checks measurable with no model and no judge, and if the score is under `PALATE_MIN_HYGIENE` (default 80) it BLOCKS and names the specific checks holding it down, ranked by points, each with its fix. **IT IS NOT A PREDICTED GRADE, AND IT WAS CALLED ONE UNTIL THE MEASUREMENT LANDED.** Across 23 fresh re-grades it correlated with the public grade at **r = -0.074** like for like, mean absolute gap **18 points**: no predictive power. **And the cause is now evidenced.** The reference was itself broken when that was taken (`capture.mjs` dropped `out.design` at its `viewportsOut` whitelist, so grades stamped `2026-08-06.design-measured` scored design on 4 of 11), which leaves the certified design dimension as the VISION HALF ALONE. Measured against that same reference, a local number that DOES carry vision (the tier-2 local grade, SigLIP head plus pairwise ladder) tracks it at **r = +0.83** over 7 sites, while this one, which carries none, sits at -0.074. **Vision is what the grade rests on, and this score has none in it by construction.** Hold the figures loosely: n = 7 puts the 95% CI on +0.83 at roughly [0.21, 0.97], and both are pre-deploy, so re-run `projected-vs-graded.mjs` once the capture fix reaches Fly. **Either way this measures HYGIENE.** Clearing 80 here means the build has no measurable hygiene faults left, and nothing more. **It is structurally blind to templating**: "Designed, not templated" (30 pts) and "One considered idea" (15) are 45 of the design dimension and neither is computable locally, so this score reads design over 32 points of denominator and the rubric's design ceiling never binds. Measured: the repo's own tidy-template fixture, three identical bordered card grids with stock copy, scores **97**. **And a clean page rests on LESS evidence, not more**: the axe checks only enter the roll-up when they FIRE, so repairing them drops the whole accessibility dimension out of the denominator (the same two fixtures go 52 -> 40 weight by being fixed, and that 97 rests on the design dimension alone). The basis thins exactly as the score rises, so a high number here is not a quality verdict, it must never be reported to a client as a predicted grade, and once nothing is left to fix the remaining work is DESIGN, which is what A.12 is for (the full local grade, see "The full local grade" below). The gate is still worth having: framework-default accents, sub-24px tap targets, sub-16px mobile body, axe violations and poor vitals are real faults and fixing them is real work. Only the claim was wrong. **The hygiene score drives a SELF-HEAL LOOP, not just a gate** (`scripts/reference-capture/hygiene-loop.mjs`): every run is persisted to `.palate-shots/hygiene-history.json`, so the block tells you the score against the floor, the ranked gaps with a fix each **and each gap's own movement**, the exact command to re-run, and **whether your last change improved or regressed it**. So the working rhythm is **fix -> rebuild -> re-run the printed command -> read the trend**, not one pass and a guess. Four rules the loop enforces so it converges instead of thrashing. (1) **A move of 2 points or less reads as UNCHANGED**, never as improvement: run-to-run spread on an unchanged page is 2.3 points (measured over five consecutive runs on the same page: 76, 78, 80, 81, 76), so celebrating a +1 would teach you that a random edit worked. (2) **Two iterations with no material gain is a STALL**, and the loop says so, names the checks that have not moved, and tells you to stop rather than run a fourth pass. Every check it scores is MECHANICAL (a hex, a font stack, a control height, a paint time), so a frozen check means the edit is not reaching the served page: confirm the source changed, the site was REBUILT, and the URL you are measuring serves the rebuild. A stall does NOT release the gate; if the floor is genuinely wrong for this build, `PALATE_MIN_HYGIENE=<n>` is recorded in the history, never silent. (3) **A run measured with different flags or routes is not compared at all** (`--no-vitals` drops 14 of the 100 weight), and a run where the scored SET changed, e.g. you fixed the contrast violation so `text_contrast` left the roll-up, reports the gain WITH the denominator caveat rather than pocketing it. (4) It **fails loud**: a corrupt history, an unwritable one, a run with no `--out`, or a non-numeric `PALATE_MIN_HYGIENE` are all said out loud, because a lost trend must never read as a clean one. `PALATE_HYGIENE_STALL_ITERS` widens the window. (The pre-rename `PALATE_MIN_GRADE` and `PALATE_GRADE_STALL_ITERS` are still honoured, with a deprecation notice, so nobody's `=0` is silently ignored.) It also runs **axe at all three viewports** and **computed-style design measurement** (framework-default accents by CIEDE2000 so a near-miss like `#7c4dff` is caught, not just an exact `#8b5cf6`; default-only type stacks; WCAG 2.5.8 tap targets; sub-16px mobile body) and **Core Web Vitals under PageSpeed's own slow-4G + 4x CPU emulation**. The build is not done until `verify-report.json` exists with `verdict: pass`. The Stop hook ENFORCES this via `gate-done.sh`: it reads the real artefacts (`verify-report.json`, `.palate-shots/*` - screenshots on disk, zero console errors, every rubric axis cleared the bar), never a manifest boolean, then folds the computed verdicts into `build-manifest.json` via `scripts/manifest-merge.mjs`. The gate enforces when it CAN (MCP connected AND a renderable preview exists) and skips otherwise (fail-open); the hook warns by default and hard-blocks under `PALATE_GATE_STRICT=1`. It also runs the **novelty gate** (`scripts/gate-novelty.mjs`, required by default via `PALATE_GATE_NOVELTY`): it blocks a safe-only CONVERGE, a near-repeat of a recent build, or a display face that recurs build-to-build, and fails-open (skips) when DIVERGE did not run or there are <2 variants / no build history. A build that fails the loop is NOT recorded to cross-build memory.
   10. **A.10 Aesthetic gates**: run `scripts/ux-lint.sh <project-dir>` (mechanical) and the reviewer pass against `references/audit-dimensions.md` (interpretive, output in Vercel `file:line` format). Fix Critical / High findings; re-run until clean. The verify-fail loop in `errors.md` applies.
   11. **A.11 Structural gate**: run `scripts/verify-is-real-astro.sh`.
   --- PREVIEW STOPS HERE ---
   11b. **A.11b THE LOCAL FULL GRADE (free, before you deploy).** Once the hygiene loop is clear, run `node scripts/reference-capture/grade-local.mjs --url <local-or-preview-url>`. This is the SAME rubric the public grader uses, with the two halves the hygiene score cannot reach: the SigLIP appearance head and the pairwise design ladder, both running locally. The ladder is judged by YOU, from a judgement request it writes; the exemplars come through the MCP. It costs nothing and needs no deploy, so it belongs in the loop, not at the end. Measured against fresh certified grades it agrees at r = +0.83 above the corpus median, against the hygiene score's r = -0.074. TWO RULES: it is a SELF-CHECK, never quotable and never shareable (a local number can be faked, which is irrelevant when you are grading your own work and fatal the moment it is published); and it WITHHOLDS its number below the corpus median, because there it is measured to flatter a weak page by up to 26 points, and a flattered score is the one error that stops someone rebuilding a site that needed it. If it withholds, treat that as the finding.

   12. **A.12 THE FULL LOCAL GRADE (once, when every gate above is green).** Run `node scripts/reference-capture/grade-local.mjs --url <url>` and act on what comes back, caps first, then findings in the order given. This is the half the hygiene gate cannot reach: it adds the SigLIP appearance head and the pairwise vision ladder against library references, scored with the grader's own `rubric.mjs`. It runs **entirely on this machine and costs nothing**, so it can be re-run after every fix. `mcp__palate__palate_grade` is INTERNAL-ONLY and will refuse: the cloud grader is funded by the free public grader, not by a customer's subscription. If the client wants a number they can show someone, they submit the URL at palatemcp.com/grade themselves, and that is the correct division, because a grade computed on inputs the builder controls proves nothing to a third party. See "The full local grade" below.

B. **Sanity (SKIP unless the build needs a CMS - see rule 7)**: first `scripts/add-sanity.sh <dir>` to wire it (no page changes), then provision project + dataset + viewer/editor tokens, SEED the dataset from `content.ts`, editor invites. The Studio is embedded at `/studio` (via `@sanity/astro`) and ships with the site - no separate Studio deploy. On `--host cloudflare`, the host overlay must already be applied: `add-sanity.sh` second, or it gets overwritten.
C. **Hosting**: Vercel (**default**) - `scripts/provision-vercel.sh` links the project, pushes env vars scoped per environment (including `GITHUB_PACKAGES_TOKEN` so the cloud build installs the private brand package), runs `vercel git connect` (so pushes auto-deploy + PRs get previews + Toolbar Comments), and triggers the initial production deploy. Cloudflare (backup, `--host cloudflare`) - `scripts/provision-cloudflare.sh` uploads Worker secrets and deploys the SSR worker. See `references/hosting-vercel.md`.
D. **GitHub**: repo, secrets, branch protection, team access, push. On `--host cloudflare`, CI builds + deploys the artifact. On Vercel, after the repo is pushed, ensure `vercel git connect --yes` has run in the project dir (provision-vercel does it when the remote already exists; otherwise run it here) so Vercel owns the deploy loop. Content changes need no redeploy on either host (SSR serves fresh - see `references/cache-invalidation.md`).
E. **Domain**: Vercel `vercel domains add` with auto-issued TLS (default); Cloudflare full-auto when on the Cloudflare backup; or external CNAME instructions. DNS cutover stays a human-confirmed step on any host.
F. **Optional**: Humblytics (or Plausible), Kit, Turnstile, IndexNow. CRO module if `--with-cro` (dormant).

### How to run a build
Provide: client name, domain, brief. Optional: `--stage=preview|production` (preview default), `--host=vercel|cloudflare` (**vercel default**; always confirmed at the plan checkpoint), design mode (clean-corporate / motion-heavy-creative / minimal-portfolio, inferred if absent), website style (product / service / story / operational / consumer / portfolio, inferred if absent), `--editors`, `--skip-brand-repo`, `--vendor-brand`, `--with-cro`, `--analytics=plausible`, `--local-preview` (hand over a local dev-server link instead of a shareable Vercel preview), infra overrides.

### Build execution order
1. **Detect environment**: `eval "$(scripts/detect-environment.sh)"`. This resolves WORK_ROOT (a proven-writable build dir that works in Cowork-as-a-task, Cowork-in-a-folder, or Claude Code in a folder) and OUTPUTS_DIR. Do all building under WORK_ROOT/{slug}-site. Never assume a path exists or is writable; this script proves it. See `references/environments.md`.
2. Confirm slug (`references/naming.md`). For production, also validate availability across Cloudflare, GitHub, the brand repo, and Sanity if the build uses a CMS.
3. **Resolve the host, then preflight matched to stage.** Host defaults to `vercel` (or `cloudflare` if chosen at the plan checkpoint); export it as `PALATE_HOST` so preflight and provisioning target the right platform. Preview runs `scripts/preflight-preview.sh` (node, npm, git, jq, writable dir, NO cloud credentials). Production runs `scripts/preflight.sh`, which checks the chosen host (Vercel CLI + `vercel whoami` by default; Cloudflare token + wrangler when `PALATE_HOST=cloudflare`) plus Sanity, GitHub, the ~/.npmrc brand token. A preview must never be blocked by missing cloud credentials it will not use.
4. **Phase 0 FIRST (brand detection sets the mode)**: brand detection or in-process BUILD BRAND (`references/phase-0-brand-detection.md`). Pin the exact version. **If the client has few usable assets** (no good site/logo/photos), run the asset-sourcing engine (`references/asset-sourcing.md`): recover and vectorise the logo, gather quality-judged real photos / about / reviews / Google Place images (reject the bad ones), fill the gaps via the imagery mode router (real > procedural > Unsplash-last, `references/asset-sourcing.md`) plus a content-gap list, and feed the imagery pattern + reviews into the Story Engine. At the END of Phase 0, DETERMINE THE BRAND MODE: set `BRAND_MODE=brand-provided` if a brand package resolved, real tokens / assets were extracted, OR the brief stated colours+fonts; else `BRAND_MODE=brand-creation` (the skill is inventing the identity). A partial brand (colour only, or a logo only) = brand-provided (LOCK the provided half, choose the missing half to fit it). Pure `--vendor-brand` defaults with no real source = brand-creation. (Phase 0 is cheap and idempotent; if it is interrupted, no `.palate-skill-state.json` exists yet, so the DIVERGE wall is dormant and nothing is trapped.)
5. **State init WITH the mode** (still before scaffold, before any source write): `scripts/state-init.sh <slug> <client> <domain> <stage> <brand_mode>`. State is created here so the build is resumable from the very first step (do not defer it; a partial scaffold must leave resumable state behind). It now runs after the cheap brand-detection step rather than before it, because the brand mode is only known once Phase 0 has run. This records `brandMode` so the DIVERGE wall can scope its axes by mode.
6. **MCP liveness probe FIRST (before any survey/compose).** Before running the Story Engine or spawning the surveyor, call `mcp__palate__refs_list_verticals` once: it is cheap and ungated, so it is a clean check that the Palate MCP is connected and its tools are named `mcp__palate__*`. If the `mcp__palate__*` tools are absent, or the call errors, do NOT stop: continue in **UNGROUNDED mode**, and state it ONCE. Tell the user, in one pass and never again: the Palate MCP is not connected, so this build carries no taste layer; reconnect with `claude mcp add --scope user --transport http palate https://mcp.palatemcp.com/api/mcp` and restart Claude Code (especially after a plugin upgrade, which can rename or drop the tools). Name what is lost, factually and without hedging: reference grounding for new design, the library taste percentile, the judging exemplars, and the certified grade. Name what still runs, because it is most of the work: the whole build, every `scripts/ux-lint.sh` rule, the rendered verifier, vitals, accessibility, and the site's own extracted token vocabulary. Then get on with the build. Favour the MCP hard - ungrounded is the degraded fallback and never a choice, so probe again if the tools appear later in the session - but a missing connection is a labelled degradation, not a wall. The depth gate records UNGROUNDED, so the absence travels with the artefact instead of hiding in it; do not repeat the warning per section, per page, or at hand-off. **If the survey is run via the `palate-surveyor` subagent** (`agents/palate-surveyor.md`), the surveyor returns a packet whose first line is the sentinel `MCP-UNAVAILABLE - ...`: that is a LOCAL-ONLY packet carrying no slugs and no donors, not a survey. Use it as the brief plus the project's own patterns, invent no donor to fill its gaps, relay the same `claude mcp add --scope user` + restart line once, and continue ungrounded. **Run the Story Engine** (`references/story-engine.md`): research the real business and its ICP, find the transformation and the one true thing, and shape the demonstrative concept each Explore variant will carry. Ground concepts and craft in the **Palate MCP** (the read path, no cloning): `refs_for_business` / `refs_match_brief` to fix the spine + style/cluster, `refs_insights { topic: "mechanics" | "emotion" }` and `refs_search { register, device, intensity }` for the concept layer, then for each donor pull a DESIGN.md (`refs_get { slug, format:"design" }`, native tokens + the WHY of each choice), `refs_get { slug, layer:"signature_moves" | "do_dont" | "component_prompts" }` for the move and taste layer, and for the spine donor `refs_get { slug, layer:"concept" }` for its demonstrative device + register, then `refs_get_astro_recipe` to make it buildable - executed via the organ-transplant method (`references/reference-library-usage.md`). `layer` (concept / pages / tokens / signature_moves / do_dont / component_prompts / astro_recipe) is the documented path; raw `sections` is legacy. **Retrieval is hybrid**, so name the exact font / technique / business category directly in the `refs_search` `query` alongside the facets (a font, "GSAP", "preloader", "optometry") - the results are quality-ranked and diversity-re-ranked. For each conversion section (pricing, booking, menu, services, contact) run the **section-build recipe** in that file: facet-search the section (`refs_search { pageType, conversionPrimitive, uiElement, query }`), VIEW donors' real inner pages (`refs_get_screenshot { slug, page:"pricing" }`), and build from their `layer:"component_prompts"` + `layer:"do_dont"` (the taste layer). A build that pulls one DESIGN.md per donor but never opens `layer:"do_dont"`, `layer:"component_prompts"`, `layer:"signature_moves"` or the inner-page screenshots is leaving the taste layer and section depth unused (the depth gate checks for those rich layers, not just call count). This step is probe + survey + ground only; the divergent concept spine is the next step.
6.1. **FREE-CAP / UPGRADE (a billing wall, not an outage and not a donor gap).** If any `mcp__palate__refs_*` call returns an error whose text mentions the limit (`used all … enriched requests`, `quota_exceeded`, or `Upgrade to Pro`), the user has hit the Palate **free tier cap** (20 deep reference reads a month). The MCP is reachable and refusing, which reads differently from a missing connection: the fix is money, not a reconnect. So **stop calling `refs_*` immediately** (every further deep read is denied anyway, and a retry loop only burns the session), keep every reference you already read, and continue LOCAL-ONLY on the same terms as UNGROUNDED mode: the build, the lint, the rendered verifier, vitals, accessibility and the site's own tokens all still run; new deep reads do not. Offer the upgrade ONCE, in these words, then finish the build without raising it again:

   > You've reached Palate's free limit (20 deep reference reads a month). Deep reads are what carry the taste layer, so the rest of this build runs on what I've already read. To ground it properly, upgrade to Pro for unlimited deep reads: open **https://app.palatemcp.com/dashboard/billing** (or https://palatemcp.com/pricing), then re-run the build. Your free allowance resets at the start of next month.

   If the survey ran via the `palate-surveyor` subagent, it returns a packet whose first line is the sentinel `QUOTA-EXCEEDED - ...` followed by whatever it legitimately read before the cap. Build from that partial survey, record in the hand-off that grounding stopped at the cap, and never invent donors to fill it out.

6.5. **MODE-AWARE DIVERGE -> CONVERGE -> COMMISSION (the mandatory first creative step, before any source file).** This is step one of the actual build and it is GATED: do NOT write a single `.astro`/`.css`/`.tsx` until it is done and recorded, because the PreToolUse wall blocks the first source write of a build site until `build-manifest.json` records a valid `diverge`+`converge` FOR THIS BRAND MODE (and the done gate catches a build site that skipped it). **DETECT the brand mode (was a brand provided?) and set the divergence axes from it.** The mode was determined at the end of Phase 0 and recorded in `.palate-skill-state.json` `brandMode` at state-init, and it MUST match `manifest.diverge.mode`. (1) **DIVERGE**: from the one true thing + spine, sample 8-10 candidate concepts in ONE pass with constraints removed (Verbalized Sampling), each self-tagging conventionality (0..1, carry >= 1 concept at <= 0.3), its lens (use 2-3 varied lenses), and a forced cross-domain analogical_seed (print, signage, architecture, film, ...). **The axes you diverge on are mode-set:**
   - **brand-creation** (NO brand provided, you are helping CREATE the brand): diverge the FULL identity space - **colourway** + **type / faces** + **mood** + **layout** + **composition** + **motion** + **density**. Put colour AND type in `axes_varied` (they are REQUIRED varied axes) and record each concept's `colourway` and `type`. The validity gate needs >= 3 distinct colourways AND >= 3 distinct type directions across the 8 (so the one-default-style failure cannot pass).
   - **brand-provided** (a brand exists): LOCK the brand colour + type + identity. Set `diverge.locked = { colour: true, type: true }` (record the palette source + faces), do NOT put colour or type in `axes_varied`, and **never vary colour or fonts away from the brand**. Diverge ONLY within the brand, on **layout** + **composition** + **section logic** + **motion** + **density** + **art direction**. The validity gate judges distinctness on those axes (>= 6 distinct layout/motion skins), NOT on the brand's face or colour.
   Write `manifest.diverge = { ran:true, n, mode, axes_varied, locked, concepts:[...] }` (full shape: `references/build-manifest.md`). (2) **CONVERGE**: score each on originality (distance from the category default AND from the last N builds) + craft-feasibility (the buildability oracle), but **combine for ORDERING, never for elimination**: feasibility is a floor (drop only the unbuildable) and the survivors are assigned to the ambition ladder, so the advanced set is the whole variant count, not one or two. **Originality is scored as DISTANCE from the default**: a direction whose type / eyebrow / heading matches the recurring reflex set named in `references/ai-slop-tells.md` (the Bricolage Grotesque + Hanken Grotesk default pairing, the status-pill eyebrow above the hero, the two-tone heading) scores near-zero originality and cannot be advanced. The default IS a recognised low-originality point, so it loses CONVERGE by construction. Write `manifest.converge = { ran:true, scored:[...], advanced:[ids] }`. When an advanced concept is audacious, CONVERGE's craft-feasibility is set by the **buildability oracle** (`references/reference-library-usage.md`): decompose into named mechanics, resolve each to its closest precedent + `astro_recipe` (or a `references/motion-and-3d.md` recipe + CURATE-gap note where the library fails closed), write `manifest.buildability`. Skip the oracle for calm conversion briefs. (3) **COMMISSION**: from the converged concept + the resolved brand, issue the build commission (A.3.5, `references/build-commission.md`): the Awwwards / FWA ambition bar, the concept + a one-line vision, the mechanisms chosen TO FIT from the toolkit register (a few not all, fit over familiarity, each grounded in its `references/motion-and-3d.md` recipe + MCP precedent), the proof requirements (1440 + 390, pixels + console, mobile-friendly, 60fps, `prefers-reduced-motion`) and the restraint clause **read in BOTH directions** (calm brand -> calm; high-intensity brand -> the bold mandate, and a flat page is a failed brief - `references/build-commission.md` "The bold mandate"). Write `manifest.commission` and carry it through Explore, Compose and the verifier. The PreToolUse wall BLOCKS the first `.astro`/`.svelte`/`.vue`/`.tsx`/`.css` write until `manifest.diverge` is valid FOR THIS MODE and `manifest.converge` advanced >= 1; the done gate catches a build site that skipped it. The commission is lightweight and fail-open (a calm brief writes a calm commission; its absence never blocks a build); DIVERGE/CONVERGE are NOT optional - they are the gated first step, and no build may begin from one default style.
7. Phase A: scaffold from the template into WORK_ROOT/{slug}-site, build the pages as real `.astro`. Then run `scripts/verify-is-real-astro.sh` (contract item 3). Mark previewVerified complete.
   - **Per Explore variant and post-Compose: run the visual loop** (`scripts/reference-capture/screenshot-build.mjs` via `palate-verifier`). A variant is not shown and a build is not done until its render clears the fixed rubric (`references/visual-rubric.md`). Serve with `scripts/serve-preview.sh`, capture `SERVE_URL`, then `node scripts/reference-capture/screenshot-build.mjs --url <SERVE_URL> --out .palate-shots --label <variant> --sections`.
8. If stage is preview: **deploy a shareable Vercel preview** with `scripts/deploy-preview.sh WORK_ROOT/{slug}-site {slug}` (add `--explore` while Explore variants are live so the picker shows), capture `SHAREABLE_URL`, and hand the person that link directly - it is a real `*.vercel.app` deployment they can send to the client for Vercel Toolbar Comments. Then STOP, noting that "make it production-ready" promotes it. With `--local-preview` (or if Vercel is unavailable), fall back to `scripts/serve-preview.sh WORK_ROOT/{slug}-site` for a local dev-server link instead.
9. If stage is production (now or later via `promote-to-production.sh`): run Phases B-F, each verified, state updated. Phase F non-blocking. **Promotion re-runs the production preflight.** `promote-to-production.sh` runs `verify-is-real-astro.sh` and then `scripts/preflight.sh` (matched to the chosen host) BEFORE it flips the stage, so a missing production credential fails loudly and early with its remediation, rather than dying mid-provision inside `provision-sanity.sh`.
10. Generate `handover.md` (`references/handover-format.md`); offer content seeding.

### Build critical rules
- The build contract above is non-negotiable. A static-HTML mockup is never an acceptable deliverable.
- Read `references/build-stages.md` before starting (preview vs production, the promotion path).
- Read `references/naming.md` before deriving any slug, `references/state.md` before any phase, `references/reference-library-usage.md` before using the library.
- Each phase MUST verify before being marked complete; preview MUST pass `verify-is-real-astro.sh`.
- Never write secrets to state. Tag resources `palate:project-slug={slug}`.
- Use the templates, not `npm create astro`. Pin the brand package to an exact version, never `latest`.
- On the Cloudflare backup, the build happens in CI and deploys an artifact; it never builds on Cloudflare. On Vercel (default), Vercel's own GitHub integration owns the build/deploy loop after the first `provision-vercel.sh` deploy.
- The reference library follows the **two-layer doctrine**: reproduce a reference's craft layer faithfully (grid, spacing rhythm, type scale, motion choreography, signature compositional move) and re-skin it with the client's brand; protect the identity layer (palette hexes, wordmark, font files, photos, copy) absolutely. The lead reference's signature move (read it via `refs_get { slug, layer:"signature_moves" }`) MUST appear in the build, re-skinned - name it and locate it. Get variety by choosing DIFFERENT references per build, not by diluting any one; vary which reference leads. Transplant the lead's structure wholesale, then graft ONE motion organ and ONE type organ from different donors via the MCP organ-transplant method (`references/reference-library-usage.md`). For every conversion section, additionally run that file's **section-build recipe** (facet-search the section, view donors' inner pages via `refs_get_screenshot { page }`, build from their `layer:"component_prompts"`, check against their `layer:"do_dont"`) so the section is grounded in how the best sites build THAT page, not memory. The client's brand colours always win.
- **Concept before composition.** Run the Story Engine (`references/story-engine.md`) before generating variants: research the real business, find the transformation and the one true thing, run DIVERGE then CONVERGE (sample wide, cull only the unbuildable, then curate the survivors onto the ambition ladder), and give each variant **its own** demonstrative concept, one per rung across the safe-warm..one-of-a-kind spectrum (both ends genuinely reached, not just the middle). Every variant passes the feel gate (would the ICP feel seen). Concept is WHAT and the feeling is WHY; reference craft is HOW, in service of the idea. These instincts are codified in `references/creative-principles.md`.
- **Reference = craft + buildability; concept layer = novelty.** The MCP tells you what is *good and buildable* (donor craft, `astro_recipe` feasibility); the concept layer plus the DIVERGE/CONVERGE spine is where *originality* comes from. Never let a donor's safe default substitute for an original concept; never let an original concept exceed what the stack can build. **When the advanced concept is audacious** (an ambitious motion / 3D / scroll-choreography moment), run the **buildability oracle** (`references/reference-library-usage.md`) before/within Compose: decompose the concept into named mechanics and resolve each to its closest real precedent + `astro_recipe`, or, where the library fails closed on a frontier mechanic (shader-surface, interactive 3D product, cursor-physics), fall back to the matching recipe in `references/motion-and-3d.md` and its perf / reduced-motion budget and note the CURATE donor gap. This keeps an ambitious idea decomposed to buildable mechanics with precedents + recipes; it fails open (a thin donor never blocks a build) and writes `manifest.buildability`. Keep it lightweight - do NOT force 3D where the brief does not call for it; a calm conversion brief skips the oracle entirely.
- **Real assets, or honest placeholders.** When the client lacks assets, the asset-sourcing engine (`references/asset-sourcing.md`) finds and quality-judges real ones (logo recovered and vectorised, good photos / about / reviews / Google Place images), fills the rest via the imagery mode router (procedural-first, Unsplash only for an unsupplied real photograph) plus a content-gap list in the handover, and feeds the imagery pattern to the Story Engine. Use real assets when good enough; never ship a bad real asset, never pass a placeholder off as the client's own. A page rendered with empty image frames reads as a wireframe, not a launched site, and FAILS the visual rubric gate.
- **The visual rubric gate (judge the render, then loop).** A build is not done until its RENDERED page clears the 6-axis rubric. After Compose (and for each Explore variant before it is shown), serve the project (`scripts/serve-preview.sh`) and screenshot it desktop + mobile with the real driver `node scripts/reference-capture/screenshot-build.mjs --url <SERVE_URL> --out .palate-shots --sections` (it targets localhost, captures full-page + per-section PNGs at deviceScaleFactor:2, and records any console error). Then score the six axes against the PIXELS using the FIXED rubric and defect checklist (`references/visual-rubric.md` - overflow, overlap, contrast, missing imagery, mobile hero legibility), plus the perceptual floors. The bar we hold to: every axis >=4, no axis below 3, no floor violation, no placeholder imagery, zero console errors. Any axis at 3 or below, or a defect, means revise the NAMED section, re-render, and re-score. **A revision is accepted only if the rubric score improves**; the critic must name a concrete defect with its location before passing an axis below 5; "no issues found" is treated as suspect and forces one more pass. Cap 2-3 iterations then escalate. Judge the artefact, not the code.
- **Run the verifier before a build is done (mandatory, in a fresh context).** Spawn the `palate-verifier` subagent (`agents/palate-verifier.md`) in an isolated context after Compose and before any preview hand-off; do not reuse the building agent's context, the verifier is the independent check. It runs every gate in one pass: MCP depth, uniqueness across the variants (`scripts/gate-uniqueness.mjs`), novelty across builds (`scripts/gate-novelty.mjs`: a safe-only converge, a near-repeat of a recent build, or a recurring display face), anti-default/slop lint, provenance mixing (no donor > half the sections; compose from at least three), the visual loop + fixed rubric (`references/visual-rubric.md`), the build result **against the commission** (`references/build-commission.md`: the proof contract at 1440 + 390, the ambition bar, the restraint clause - fit over maximal motion), and verify-is-real-astro. It writes `verify-report.json` (computed evidence, not narration) and returns one pass/fail report; fix the named sections and re-verify, capped at 2-3 cycles then escalate. The deterministic gates are authoritative. The build is not done until `verify-report.json` exists with `verdict: pass`.

### The full local grade (`grade-local.mjs`)

**The hygiene gate does not predict the public grade, and the gap is structural.** Measured on 2026-08-06 across 23 fresh re-grades: like-for-like correlation **r = -0.074**, mean absolute gap **18 points**, per-site error up to **42**, the same band once in 23. janemackenzie.co.uk projects **86** and grades **44**. The cause is not calibration: design carries weight 40 and rests on the SigLIP appearance head and the pairwise vision ladder, so a gate carrying no vision measures hygiene while the grade is mostly design.

The fix is not to call a remote instrument. It is to run the vision half locally, which is what `grade-local.mjs` does, and it correlates **r = +0.83** with the certified grade across 7 sites precisely because it carries vision. So:

- **The hygiene gate is the INNER loop.** Free, seconds, run constantly. Passing it means nothing measurable is broken. It is a floor, never a prediction.
- **The full local grade is the DONE-TIME check.** Also free, also on this machine, a few minutes. Same `rubric.mjs`, same judging prompt, same appearance head as the certified grader.
- **The certified grade is the WEBSITE.** palatemcp.com/grade, run by whoever needs a number they did not compute themselves.

**Why `palate_grade` refuses**

It is internal-only. A certified grade is about US$1.06 of our infrastructure, roughly 100 seconds of a dedicated machine running Chromium plus the vision calls, and that spend is what pays for the free public grader where a stranger's URL buys a lead. Sold through the MCP it loses money: at the old pro allowance this one tool could bill $159 a month against a $9 subscription. Nothing of substance is lost, because the local grade runs the same instrument on hardware the customer already owns. What it cannot do is prove anything to a third party, and that is the line worth keeping.

**How to use the local grade**

1. **Two phases, because you are the judge.** `grade-local.mjs --url <url>` captures, measures, embeds and writes a judgement request naming six pairwise comparisons. Answer them, then `grade-local.mjs --judgements <file>` scores with the rubric.
2. **Judge each comparison in a FRESH context.** The two orderings of a pair are a control; an agent that judges both in one context is asking itself to agree with itself. If you genuinely cannot, pass `--not-independent` so the result says so rather than overstating itself.
3. **Be blunt.** Most sites are clearly worse than a library reference and saying so is the job. Do not soften a verdict because you built the site.
4. **Check the exemplars actually rendered.** Some stored screenshots are blank; a blank reference silently FLATTERS the candidate, which is the one failure mode that cannot be noticed from the score.
5. **Act on `caps` first, then `findings` in the order given.** Findings are ranked by points recoverable across the whole rubric, so a 0.2 on a 30-point check outranks a 0 on a 5-point one.
6. **The design ceiling binds most builds.** Overall cannot exceed design craft by more than 15, so hygiene fixes cannot lift a site whose design is holding it down. If design is the cap, the fix is structural, not schema markup and image compression.
7. **Never report a local score as a grade.** It is computed here, on inputs we control, so it is a self-check. Do not put it in a proposal, next to a competitor, or in front of a client as a grade. If they want that number, they submit the URL at palatemcp.com/grade.

---

## Mode: Continue an existing site

The plugin as the ongoing partner, not just the scaffolder: add a page or section, restyle, extend copy, add a feature, on a site that already exists. It carries the SAME taste rigour as a fresh build (MCP-grounded per page, the anti-slop + rendered + visual-rubric gates, SEO on every page) MINUS the identity + scaffold machinery. It works whether or not the plugin built the site (a Palate scaffold, a Webflow migration it shipped, or a hand-built site it has never seen). Full doctrine: `references/continue-mode.md`.

### The rule that defines the mode
Identity is LOCKED. CONTINUE is always brand-provided: it NEVER runs DIVERGE / CONVERGE on colour, type or identity, because every addition must look like it was always part of the site. Diverge only WITHIN the locked brand (layout / motion / density) when a new section needs options. The existing site's brand always wins, so the mode never re-runs DIVERGE on identity. (The PreToolUse DIVERGE wall is scoped to an active BUILD SITE and structurally exempts edits + external sites; `references/continue-mode.md` has the exact mechanics.)

### The loop (per page or section)
1. **Orient**: resolve the project; detect provenance (Palate-scaffolded -> inherit its brand-record + state; external -> LOCK the site's own tokens / Tailwind preset / CSS vars / layout). Read the conventions the addition must match (framework, layout + SEO wiring, token source, components, routing, nav, sitemap). New work is surgical and native.
2. **MCP liveness probe** (`refs_list_verticals`) - if the `mcp__palate__*` tools are absent, say so ONCE with the recovery line (`claude mcp add --scope user --transport http palate https://mcp.palatemcp.com/api/mcp` + restart) and continue in UNGROUNDED mode: no reference grounding for the new work, no taste percentile, no judging exemplars, no certified grade; the lint, the rendered verifier, vitals, accessibility and the site's own tokens are unaffected. The depth gate records UNGROUNDED, so it is labelled rather than silent. Honour the free-cap wall (6.1) the same way: local-only, one upgrade offer, no further `refs_*` calls.
3. **Ground per page/section** via the section-build recipe (`references/reference-library-usage.md`): `refs_search { pageType, uiElement, conversionPrimitive, query }` -> view a donor's real inner page (`refs_get_screenshot { page }`) -> build from its `layer: signature_moves / do_dont / component_prompts` + `refs_get_astro_recipe`, re-skinned to the locked brand.
4. **Build as real code IN the project**, matching its conventions and reusing its components + tokens (never a parallel design language, never a loose HTML file).
5. **SEO on the page**: real title / description / canonical / social card, added to the sitemap + nav where it belongs, with the structured data the site uses.
6. **Gate the CHANGE** (only what applies to an edit): `scripts/ux-lint.sh` on the changed files, then the rendered gates on the CHANGED page(s), `scripts/verify-rendered.sh` + the visual rubric via `scripts/reference-capture/screenshot-build.mjs` at 1440 + 390, judged against `references/visual-rubric.md` + `references/ai-slop-tells.md` (spawn `palate-verifier`, scoped to the changed page). SKIP: DIVERGE/CONVERGE/EXPLORE, scaffold + `verify-is-real-astro.sh`, and Phases B-F (the infra already exists).

### The plan checkpoint
Show a short plan and get a go for a SUBSTANTIAL addition (a whole new page, a nav / IA change, anything touching money / forms / auth). SKIP it for tiny reversible work (one section, a copy fix, a restyle). Do not make a quick edit wait on a confirmation.

---

## Mode: Build a brand package

Converts raw brand assets into a published private npm package `@palate-projects/{slug}-brand`: four-format design tokens (W3C DTCG JSON source of truth, CSS custom properties, typed TS, Tailwind preset), a unified fonts.css, kebab-cased assets, reference React + Tailwind components, full-page examples, and seven markdown brand docs. The package feeds Claude Design, v0, Cursor, Figma Make, and is consumed by the BUILD SITE mode.

Usable two ways:
- **Standalone**: asked for a brand package, no website. Interactive: ask before inventing when assets are missing.
- **Composed**: called by BUILD SITE Phase 0. Never blocks the flow; uses documented defaults, batches assumptions for the handover.

### Brand needs
A path to raw assets (brand PDF, fonts, logos, illustrations, copy, any existing brand skill), client name and slug.

### Brand execution order
1. Preflight: `scripts/brand-preflight.sh` (gh auth, packages-write capability, node, npm, sips).
2. Resolve repo: `scripts/resolve-repo.sh {slug}` (create if absent, verify empty if present).
3. Init state: `scripts/brand-state-init.sh` (`.palate-brand-state.json`).
4. Inventory assets: `scripts/inventory-assets.sh` (`references/brand/asset-classification.md`). Reuse any existing brand/voice doc verbatim.
5. Scaffold: copy `templates/brand-repo-skeleton/` (`references/brand/structure.md`).
6. Process assets: `scripts/process-images.sh` (kebab-case, AVIF+JPG pairs).
7. Generate tokens: one logical source to four formats (`references/brand/token-generation.md`).
8. Write fonts.css: one family per typeface (`references/brand/fonts-css-rules.md`).
9. Write components, examples, the seven brand docs.
10. Write meta files: README, CLAUDE.md, AGENTS.md, LICENSE, package.json, .npmrc, .gitignore.
11. Publish: `scripts/publish-package.sh` (registry-check first, skip-if-identical or patch-bump).
12. Commit and push (ask if interactive; proceed if composed and approved upfront).
13. Photography pass (separate, if photography present).

### Brand critical rules
- Read `references/brand/structure.md` before scaffolding, `references/brand/opinionated-choices.md` before tokens/fonts/photography.
- Reuse existing voice/brand work verbatim.
- Never invent silently: interactive asks, composed batches and defaults.
- One family name per typeface. Tokens from one source, four formats, never hand-written separately.
- Consumers pin exact versions. Never push without approval.
- The brand repo's CLAUDE.md leads with brand-specific non-negotiables (colour, language e.g. Australian English, typography e.g. no em dashes, character rules).

---

## Shared infrastructure

- **Slug derivation**: `scripts/derive-slug.sh` (shared across both modes; handles the digit-start edge case).
- **Reference library**: the Palate MCP serves the catalog of deeply-analysed sites. BUILD SITE reads it through the hosted **Palate MCP** (`refs_*` tools via the `palate` connector), not by cloning. See `references/reference-library-usage.md`.
- **State**: BUILD SITE uses `.palate-skill-state.json`; BUILD BRAND uses `.palate-brand-state.json`. Both resumable. The cross-mode handshake (a build that called a brand build, interrupted mid-brand) is in `references/phase-0-brand-detection.md`.

## Known gotchas

See `references/gotchas.md` (build) and `references/brand/gotchas.md` (brand). The critical ones: the CI workflow needs `permissions: packages: read` or the brand package install 401s; set GitHub secrets before first push; the Sanity Studio hostname is globally unique; the build never happens on Cloudflare; GitHub Packages needs a `write:packages` PAT for the brand mode; the brand pin intentionally lags the brand repo latest.

## References

Build operational: `build-stages` (preview vs production, the contract), `continue-mode` (the ongoing-build mode: add / edit / extend an existing site), `cms-and-draft-preview` (the SSR + Sanity + draft-mode architecture - read before any build), `production-handoff` (running provisioning hands-off via the user's Terminal), `naming`, `state`, `errors`, `idempotency`, `pipeline`, `phase-0-brand-detection`, `story-engine` (the divergent spine), `build-commission` (the ambition bar + the toolkit, issued post-CONVERGE / pre-Explore), `explore-stage`, `critique-discipline` (the four pre-emit habits), `audit-dimensions` (the reviewer pass), `anti-patterns` (the ux-lint rule source of truth), `build-memory` (cross-build diversification), `industry-patterns` (per-industry anti-patterns), `motion-and-3d` (the recipe layer, incl. the proven R3F hero recipe), `rendered-bug-classes` (the BOLD-build defects + the `verify-rendered.sh` gate that catches them), `brand-package-consumption`, `reference-library-usage`, `existing-infra`, `hosting-vercel`, `team-access`, `composition`, `failure-path` (what the person is shown when local breaks, and why a broken working copy is never an outage), `roles` (the three roles and the review routing that decides which change needs a human).
Build detail: `assets`, `connective-tissue`, `testing`, `environments`, `cache-invalidation`, `schema-versioning`, `claude-md-template`, `agents-md-template`, `handover-format`, `versioning`, `webflow-migration`.
Brand (`references/brand/`): `structure`, `opinionated-choices`, `perceptual-floors` (cited research-grounded minimums), `normalisation-rules` (raw extraction to usable brand), `asset-classification`, `token-generation`, `fonts-css-rules`, `photography-pipeline`, `voice-extraction`, `publishing`, `state`, `gotchas`, `handback`.
Library: `reference-library-usage` (how BUILD SITE consumes the Palate MCP's library).
CRO: `cro-module`. Reference: `stack`, `gotchas`, `examples`.

## Scripts

Build: `detect-environment` (run first; resolves a writable WORK_ROOT for any environment), `preflight-preview` (preview: node/npm/git/jq only), `preflight` (production: adds cloud creds), `derive-slug`, `detect-brand-repo`, `verify-brand-exports`, `state-init/update/resume`, `verify-is-real-astro` (anti-freestyle structural gate), `ux-lint` (anti-slop aesthetic gate, parses `references/anti-patterns.md`), `deploy-preview` (default preview handover: deploys a SHAREABLE Vercel preview + Toolbar Comments; `--explore` shows the picker), `serve-preview` (the `--local-preview` fallback: local dev-server link), `promote-to-production`, `add-sanity` (the OPT-IN CMS overlay - run only when the build needs a CMS, and after the host overlay), `provision-sanity/github`, `provision-vercel` + `verify-vercel` (the default host), `switch-host-cloudflare` + `provision-cloudflare` + `verify-cloudflare` (only on `--host cloudflare`), `attach-domain`, `optional-services`, `install-cro`, `verify-*`, `rollback`.
Brand: `brand-preflight`, `resolve-repo`, `brand-state-init/update/resume`, `inventory-assets`, `process-images`, `publish-package`.

## Templates

`templates/astro-project/` (the site scaffold, build-in-CI workflows, committed .npmrc, plus `_claude/hooks/anti-slop-check.js` + `_claude/settings.local.json` that Phase A merges into `.claude/` so every shipped project carries the project-level anti-slop gate), `templates/host-cloudflare/` (the Cloudflare backup overlay - adapter, deps, `wrangler.toml`, `public/_headers`, the wrangler CI workflows - applied by `switch-host-cloudflare.sh` only on `--host cloudflare`; the baseline `astro-project/` is Vercel-native and ships `ci.yml` (the compile check) plus `palate-contract.yml` (the contribution gate, run on every pull request) and `palate-sweep.yml` (the weekly site-level sweep)), `templates/brand-repo-skeleton/` (the brand package shape, four-format tokens, components, examples), `templates/cms-sanity/` (the OPT-IN CMS overlay applied by `add-sanity.sh`: the Sanity-wired `astro.cms.mjs` + `CmsVisualEditing.astro` + `load.ts` that replace the base no-ops, plus `sanity.config.ts`, the schemas, the image helper, the seed scripts and `deps.json`; the base scaffold carries none of it), `templates/sanity-schema/` (collections incl. formSubmission, heroVariant, campaignPage), `templates/github-workflows/`, `templates/cro/` (the four dormant CRO sub-skills as .tpl, renamed on install).

## Evals

`evals/` holds canonical test briefs with objective pass/fail checklists - run them after any change to the skill to catch regressions before they reach a client build. `evals/01-brochure-preview.md` through `04-explore-stage.md` cover the core build flows; `evals/05-ux-lint-critical-rules.md` through `10-brand-normalisation.md` cover ux-lint, critique discipline, perceptual floors, the project-level hook, the reviewer pass, and brand normalisation (one eval per file).

## Examples

See `references/examples.md`. Across both modes:
- Build: "Build a site for Addikted to Ink, atink.com.au. Brand assets at ~/Downloads/atink-brand."
- Brand only: "Build a brand package for Enviz, assets at ~/Downloads/enviz-brand."
